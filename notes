;; -*- mode: outline; outline-regexp: " *[-\+]"; indent-tabs-mode: nil -*-

^c^d hide subtree
^c^s show subtree


- next
 - :object mode
  - if not one of the normal types, check for other to_json and to_hash
  - on load if there is a json_class but not a ^o then check for json_create
   - to_json and to_hash should use json_class

 - support json_create()
  - use this to allow users to support additional classes like Complex, Rational, or Regexp
  - JSON.create_id = '^o' can be used for compatibility
  - also look for json_class in compat mode

 - circular reference


 - stream
  - load
  - dump

- load

- dump
 - multibyte group encoding
 - object and other types of object dumping


- support circular object encoding

